import { env } from "cloudflare:workers";
import { notFound } from "next/navigation";
import { ArrowLeft, Download } from "lucide-react";
import { SiteFooter } from "../../site-footer";
export const dynamic="force-dynamic";
type Article={id:number;title:string;excerpt:string;body:string;platform:string;kind:string;createdAt:string;downloadName:string|null;youtubeId:string|null;imageName:string|null};
export default async function ArticlePage({params}:{params:Promise<{id:string}>}){const {id}=await params;let a:Article|null=null;try{if(env.DB)a=await env.DB.prepare(`SELECT id,title,excerpt,body,platform,kind,created_at AS createdAt,download_name AS downloadName,youtube_id AS youtubeId,image_name AS imageName FROM posts WHERE id = ? AND status = ?`).bind(Number(id),"published").first<Article>()}catch{}if(!a)notFound();return <main className="article-page"><header className="sub-header"><a className="brand" href="/"><span className="brand-mark"><img src="/goldengames-logo.png" alt=""/></span><span><b>GOLDENGAMES</b><em>NEXUS v1.0</em></span></a><a href="/"><ArrowLeft size={17}/> Back to news</a></header><article><div className="article-meta"><b>{a.platform}</b><span>{a.kind}</span><time>{new Date(a.createdAt).toLocaleDateString("en",{month:"long",day:"numeric",year:"numeric"})}</time></div><h1>{a.title}</h1><p className="article-lead">{a.excerpt}</p>{a.youtubeId?<div className="article-video"><iframe src={`https://www.youtube-nocookie.com/embed/${a.youtubeId}`} title={a.title} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/></div>:a.imageName?<div className="article-cover"><img src={`/api/images/${a.id}`} alt=""/></div>:<div className="article-visual"><strong>{a.platform}</strong><span>GOLDENGAMES NEXUS</span></div>}<div className="article-body">{a.body.split(/\n+/).filter(Boolean).map((p,i)=><p key={i}>{p}</p>)}</div>{a.downloadName&&<div className="article-download"><div><span className="eyebrow">ATTACHED DOWNLOAD</span><b>{a.downloadName}</b></div><a className="primary-btn" href={`/api/files/${a.id}`}><Download size={17}/> Download file</a></div>}<aside className="article-disclaimer"><b>Educational-use disclaimer</b><p>This content is shared strictly for educational and research purposes. It does not encourage piracy, copyright infringement or violations of platform terms. You are responsible for following applicable laws and protecting your own devices and data.</p></aside></article><SiteFooter/></main>}
