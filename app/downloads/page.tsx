import { env } from "cloudflare:workers";
import { ArrowLeft, Download, FileArchive } from "lucide-react";
import { SiteFooter } from "../site-footer";
export const dynamic="force-dynamic";
type Item={id:number;title:string;excerpt:string;platform:string;downloadName:string;downloadSize:number;createdAt:string};
export default async function Downloads(){let items:Item[]=[];try{if(env.DB){const r=await env.DB.prepare(`SELECT id,title,excerpt,platform,download_name AS downloadName,download_size AS downloadSize,created_at AS createdAt FROM posts WHERE status = ? AND download_key IS NOT NULL ORDER BY created_at DESC`).bind("published").all<Item>();items=r.results}}catch{}
return <main className="sub-page"><header className="sub-header"><a className="brand" href="/"><span className="brand-mark"><img src="/goldengames-logo.png" alt=""/></span><span><b>GOLDENGAMES</b><em>NEXUS v1.0</em></span></a><a href="/"><ArrowLeft size={17}/> Back to news</a></header><section className="sub-hero"><span className="eyebrow">DOWNLOAD LIBRARY</span><h1>Community files,<br/>organized clearly.</h1><p>Check the platform, article notes and compatibility information before using any download.</p></section><section className="downloads-list">{items.length?items.map(i=><article key={i.id}><div className="file-icon"><FileArchive/></div><div><span>{i.platform}</span><h2>{i.title}</h2><p>{i.excerpt}</p><small>{i.downloadName} • {formatBytes(i.downloadSize)}</small></div><a className="primary-btn" href={`/api/files/${i.id}`}><Download size={17}/> Download</a></article>):<div className="empty-state"><FileArchive size={30}/><h3>No public downloads yet</h3><p>Files attached to published updates will appear here automatically.</p></div>}</section><SiteFooter/></main>}
function formatBytes(n:number){if(!n)return"Unknown size";const u=["B","KB","MB","GB"];const i=Math.min(Math.floor(Math.log(n)/Math.log(1024)),3);return`${(n/1024**i).toFixed(i?1:0)} ${u[i]}`}
